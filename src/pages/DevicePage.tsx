import React from 'react';

import Box from '@mui/material/Box';

type Props = {
  children?: React.ReactNode;
};

const DevicePage = (props: Props) => {
  return <Box>This is the device page</Box>;
};

export default DevicePage;
