import React from 'react';

import Box from '@mui/material/Box';

type Props = {
  children?: React.ReactNode;
};

const AnnouncementPage = (props: Props) => {
  return <Box>This is the announcement page</Box>;
};

export default AnnouncementPage;
